[build]
  command = "npm ci --legacy-peer-deps && npm run build && ls -la dist && (ls -la dist/assets || true)"
  publish = "dist"

# Falls du React Router / Client-Routing nutzt:
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200